<script>
    // this will be similar to the BlogGrid component, but will need different aspect ratios, breakpoints, and (probably) different pagination rules. Could still just use one component with sufficiant parameters to conditionally change the layout, but that sounds like a serious headache...
</script>

<div />
